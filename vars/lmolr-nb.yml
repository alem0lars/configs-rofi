# => inherits: generic, with-lodpi <= #

font:
  name: 'Noto Sans'
  size: 12
terminal: openterm
cmd:
  ssh: "{terminal} --cmd '{ssh-client} {host}'"
  shell: "{terminal} --cmd '{cmd}'"
theme: glue_pro_blue

# vim: set filetype=eruby.yaml :