# => inherits: generic <= #


font:
  name: Hack
  size: 10
terminal: openterm
cmd:
  ssh: "{terminal} --cmd '{ssh-client} {host}'"
  shell: "{terminal} --cmd '{cmd}'"
theme: glue_pro_blue
dpi: 192


# vim: set filetype=eruby.yaml :
